[package]
name = "gloss"
version = "0.1.0"
edition = "2021"
description = "划词翻译工具 - 纯 Rust + egui + WebGPU"
license = "MIT"

[dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
