# unwrap_used / expect_used / panic 在测试代码（#[test] 与 #[cfg(test)]）中放行：
# 测试断言天然依赖「失败即 panic」，错误传播反而是噪音。
allow-unwrap-in-tests = true
allow-expect-in-tests = true
allow-panic-in-tests = true
# print_stdout / print_stderr 在测试代码中放行：调试输出与失败现场打印只发生在测试里。
allow-print-in-tests = true
