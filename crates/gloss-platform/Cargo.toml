[package]
name = "gloss-platform"
description = "Gloss 适配器层：实现 core 端口 + 平台事件源"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
gloss-core = { path = "../gloss-core" }
