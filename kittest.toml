# egui_kittest 快照对比配置（L2 浮层 harness 测试）
# 跨平台渲染差异用分平台阈值吸收；输出路径相对各 crate 的 manifest 目录。
output_path = "tests/snapshots"
threshold = 0.6
max_failed_pixels = 0

[mac]
threshold = 0.6
max_failed_pixels = 0
